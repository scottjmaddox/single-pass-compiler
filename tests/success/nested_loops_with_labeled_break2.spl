const let foo = fn() -> u1 { 0:u1 }

const let main = fn() {
    let outer_result: u3 = 'outer: loop {
        let inner_result: u2 = 'inner: loop {
            if foo() {
                break 'outer 1:u3
            } else {
                break 'inner 2:u2
            }
        }
        if inner_result != 2 { __builtin_trap() }
        break 3
    }
    if outer_result != 3 { __builtin_trap() }
}
