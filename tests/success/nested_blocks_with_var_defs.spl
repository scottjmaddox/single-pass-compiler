
const let main = fn() {
    let outer_result = {
        let inner_result = 1:i32
        if inner_result != 1 { __builtin_trap() }
        2:i32
    }
    if outer_result != 2 { __builtin_trap() }
}
