error: expected type `i2` from:
 --> tests/failure/expected_subtype_i2_i1.spl:2:27
  |
  | const main = fn() -> i1 { foo() }
  |                           ^^^^^
  to be a subtype of type `i1` from:
 --> tests/failure/expected_subtype_i2_i1.spl:2:22
  |
  | const main = fn() -> i1 { foo() }
  |                      ^^
  note: type A is a subtype of type B if and only if:
  - every value that A can represent is representable by B, and
  - type A supports all the operations that type B supports
